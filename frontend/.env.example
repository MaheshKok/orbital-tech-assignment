# Orbital Copilot Frontend Environment Variables
# Copy this file to .env.local for local development

# Backend API URL
# Leave empty to use Vite proxy in development (recommended)
# Set to full URL for production or direct API access
VITE_API_URL=

# Development proxy target (used by vite.config.ts)
# Only used when VITE_API_URL is empty
BACKEND_URL=http://localhost:8000
